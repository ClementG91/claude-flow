{
  "name": "claude-flow",
  "version": "0.1.0",
  "description": "Visual workflow manager for Claude Desktop scheduled tasks — n8n-style task orchestration",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @claude-flow/server dev",
    "build": "pnpm -r build",
    "test": "pnpm --filter @claude-flow/core test",
    "lint": "pnpm exec eslint . --ext .ts,.tsx --ignore-pattern dist --ignore-pattern node_modules",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.0.0",
    "playwright": "^1.61.0"
  },
  "keywords": [
    "claude",
    "workflow",
    "scheduled-tasks",
    "automation",
    "n8n",
    "react-flow"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
